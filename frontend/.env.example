# ===========================================
# STAT-DISCUTE MVP - Environment Configuration
# ===========================================
# Copy this file to .env.local and fill in your values
# cp .env.example .env.local

# ===========================================
# REQUIRED: Database Configuration
# ===========================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nba_stats
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# ===========================================
# REQUIRED: JWT Authentication (EdDSA - Ed25519)
# ===========================================
# Generate new keys for production:
# openssl genpkey -algorithm Ed25519 -out private.pem
# openssl pkey -in private.pem -pubout -out public.pem
# Then copy contents here (replace newlines with \n)

JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"
JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\nYOUR_PUBLIC_KEY_HERE\n-----END PUBLIC KEY-----\n"
JWT_ACCESS_TOKEN_EXPIRES_IN=15m
JWT_REFRESH_TOKEN_EXPIRES_IN=7d
JWT_ISSUER=stat-discute.be
JWT_AUDIENCE=stat-discute-api

# ===========================================
# OPTIONAL: Security Configuration
# ===========================================
# Defaults are reasonable for production
RATE_LIMIT_LOGIN_MAX_ATTEMPTS=10
RATE_LIMIT_LOGIN_WINDOW_MS=900000
RATE_LIMIT_ACCOUNT_MAX_ATTEMPTS=5
RATE_LIMIT_ACCOUNT_WINDOW_MS=900000
ACCOUNT_LOCKOUT_DURATION_MS=1800000
BCRYPT_ROUNDS=12

# ===========================================
# POST-MVP: Optional Features (Not Required)
# ===========================================
# Uncomment and configure when enabling these features

# AI Chat (requires Ollama server)
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.2

# Anthropic AI (alternative to Ollama)
# ANTHROPIC_API_KEY=sk-ant-...

# ML Predictions API
# NEXT_PUBLIC_ML_API_URL=http://localhost:8000
# ML_SCRIPTS_PATH=/path/to/ml/scripts
# PYTHON_PATH=/usr/bin/python3

# Betting Agent
# BETTING_AGENT_URL=http://localhost:8001

# App URL (for password reset emails)
# NEXT_PUBLIC_APP_URL=https://stat-discute.be
