// New transition system (recommended)
export { RevealOverlay } from './RevealOverlay'

// Backward compatibility exports (deprecated, use TransitionContext instead)
export { PageTransitionProvider, usePageTransition } from './PageTransition'
